services:
  app:
    image: oven/bun:latest
    working_dir: /app
    volumes:
      - ./backend:/app
      - /app/node_modules
      - ./backend/data:/app/data # ここにDBが保存されている
    ports:
      - "3000:3000"
    command: sh -c "bun install && bun run server.js"
